---
import Layout from '../../layouts/Layout.astro';
import AdminDashboardClient from '../../components/admin/AdminDashboardClient';

export const prerender = false;
---

<Layout
  title="Panel de Administración | Bella Forever"
  description="Administración de catálogo, productos y categorías de Bella Forever."
  noindex={true}
>
  <AdminDashboardClient client:load />
</Layout>
